import { NotFoundAlert } from "@/components/layout/not-found-alert";

const RoomNotFound = () => (
  <NotFoundAlert title="Room doesn't exist" description="This room doesn't exist or you are not invited." />
);

export default RoomNotFound;
